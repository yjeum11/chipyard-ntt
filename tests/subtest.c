#include <inttypes.h>
#include <stdlib.h>
#include "rocc.h"
#define USE_ROCC

int main() {
  uint64_t a = 70;
  uint64_t b = 30;
  uint64_t c;

  if (f == 3 && cf == 1 && df == 1) {
    return 0;
  } else {
    return 1;
  }
}
